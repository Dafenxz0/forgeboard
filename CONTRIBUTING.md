# Contributing

Forgeboard is an early prototype. Focused pull requests are welcome.

Good first contribution areas:

- improve scoring fixtures
- add a GitHub adapter interface
- add more tests around score boundaries
- improve responsive layout
- draft JSON schema for exported reports

Run checks before opening a pull request:

```bash
npm test
npm run build
```
